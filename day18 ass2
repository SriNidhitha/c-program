#include<stdio.h>
main()
{
	int n,r,sum=0,c,temp;
	scanf("%d",&n);
	n=r;
	while(n>0)
	{
		r=n%10;
		n=temp/10;
		r=temp;
		c=r*r*r;
		sum=sum+c;
	}
	n=temp;
	if(n==sum)
	printf("armstrong");
	else
	printf("not armstrong");
}
